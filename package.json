{
  "name": "dayjaview-product",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @dayjaview/web",
    "build": "npm run build --workspace @dayjaview/web",
    "typecheck": "npm run typecheck --workspaces --if-present"
  }
}

