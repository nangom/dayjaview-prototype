# 디자인 시스템 경계

## 디자인 토큰

색상, 글꼴, 글자 크기, 간격, 곡률, 테두리, 모션 시간처럼 여러 화면에서 반복되는 원자 값을 정의한다.

## UI 컴포넌트

토큰을 사용해 앱바, 버튼, 하단 내비게이션, 테마 행, 상태 라벨을 구현한다. 컴포넌트는 DAY-JA-VIEW의 테마 순위나 수익률을 계산하지 않는다.

## 제품 화면

화면은 컴포넌트를 조합하고 route, fixture, REST, WebSocket 상태를 연결한다. 서버가 준 수익률, 순위, 표본 수를 다시 계산하지 않는다.

